package com.yorbax.EMA.lecturer.activity;import android.content.Intent;import android.os.Bundle;import android.view.View;import android.widget.Toast;import androidx.annotation.NonNull;import androidx.appcompat.app.AppCompatActivity;import androidx.recyclerview.widget.LinearLayoutManager;import androidx.recyclerview.widget.RecyclerView;import com.google.android.material.floatingactionbutton.FloatingActionButton;import com.google.firebase.auth.FirebaseAuth;import com.google.firebase.database.DataSnapshot;import com.google.firebase.database.DatabaseError;import com.google.firebase.database.FirebaseDatabase;import com.google.firebase.database.ValueEventListener;import com.yorbax.EMA.R;import com.yorbax.EMA.lecturer.ExamAdapter;import com.yorbax.EMA.lecturer.model.MCQsModel;import com.yorbax.EMA.lecturer.model.McqsDetail;import java.util.ArrayList;import java.util.List;public class ManageExam extends AppCompatActivity {    FloatingActionButton floatingActionButton;    RecyclerView rv_lec;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_manage_exam);        floatingActionButton = findViewById(R.id.floatingActionButton);        rv_lec = findViewById(R.id.recyclerView);        floatingActionButton.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View view) {                startActivity(new Intent(ManageExam.this, AddExam.class));            }        });        manageCourse();    }    void manageCourse() {        List<MCQsModel> mcQsModelList = new ArrayList<>();        FirebaseDatabase.getInstance().getReference().child("Exam").addListenerForSingleValueEvent(new ValueEventListener() {            @Override            public void onDataChange(@NonNull DataSnapshot snapshot) {                mcQsModelList.clear();                for (DataSnapshot snapshot1 : snapshot.getChildren()) {                    if (snapshot1.child("lectureId").getValue(String.class).equals(FirebaseAuth.getInstance().getUid())) {                        String id = snapshot1.getKey();                        String ExamId = snapshot1.child("ExamId").getValue(String.class);                        String totoalMcsq = snapshot1.child("totoalMcsq").getValue(String.class);                        String totalTime = snapshot1.child("totalTime").getValue(String.class);//                     mcqsDetails = snapshot1.child("mcqsDetails").getValue(McqsDetail.class);                        List<McqsDetail> mcqsDetails1 = new ArrayList<>();//                    mcqsDetails1.add(mcqsDetails);                        String lectureId = snapshot1.child("lectureId").getValue(String.class);                        mcQsModelList.add(new MCQsModel(id, totoalMcsq, totalTime, mcqsDetails1, lectureId));                    }                }                rv_lec.setLayoutManager(new LinearLayoutManager(ManageExam.this));                ExamAdapter lectureAdapter = new ExamAdapter(ManageExam.this, mcQsModelList);                rv_lec.setAdapter(lectureAdapter);            }            @Override            public void onCancelled(@NonNull DatabaseError error) {                Toast.makeText(ManageExam.this, error.getMessage(), Toast.LENGTH_SHORT).show();            }        });    }}